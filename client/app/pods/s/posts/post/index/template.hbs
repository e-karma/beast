<div class="container">
	<div class="page-header">
        <h2>{{model.title}}</h2>
    </div>
    <p>
		{{#each tags as |tag|}}
		<span class="badge">
			{{tag}}
		</span>
		{{/each}}
	</p>
	<p>{{model.body}}</p>
	{{#link-to 's.posts.post.edit' classNames="btn rb-btn-info"}}edit{{/link-to}}&nbsp;

</div>
